<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.InohomTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>

    <style name="GradientButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@null</item>
        <item name="android:background">@drawable/bg_tile_gradient</item>
        <item name="cornerRadius">12dp</item>
    </style>
</resources>